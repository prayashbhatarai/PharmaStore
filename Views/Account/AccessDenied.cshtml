﻿@{
    Layout = "_BasicLayout";
    var returnUrl = ViewData["ReturnUrl"];
    ViewData["Title"] = "Access Denied";
}
<style>
    body {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: white;
    }

    .card {
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
</style>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-6 col-sm-8">
            <div class="card shadow-lg p-3 mb-5 bg-dark rounded">
                <div class="card-body text-center">
                    <div class=" mx-auto container d-flex justify-content-center">
                        <img src="~/img/PharmaStore.svg" class="img-fluid" alt="Pharma Store">
                    </div>
                    <h3 class="text-danger">Access Denied</h3>
                    <p class="lead text-light fst-italic">Sorry, you don't have permission to access this page.</p>
                    <div class="text-center">
                        <button type="button" class="m-2 btn btn-primary" onclick="history.back()">Go back to previous page</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>